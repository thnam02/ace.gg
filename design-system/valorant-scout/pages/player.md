# Player detail overrides

> Overrides `MASTER.md` for `/players/[id]`.

- Dense identity header (handle, team, region, rank) then a 2–3 column stat grid.
- Include a Compare CTA that preselects this player when possible (`/compare?ids={id}`).
- 404: clear message + link back to roster. No blank page.
