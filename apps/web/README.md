# VALORANT Scout web

Dense stats console. Talks to the FastAPI service.

```bash
cd apps/web
cp ../../.env.example ../../.env  # optional; API URL defaults to http://localhost:8000
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). The API must be running on port 8000.
